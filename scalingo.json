{
  "scripts": {
    "postdeploy": "./postdeploy.sh"
  },
  "env": {
    "AUTOMETA_ENV": {
      "description": "Deployment environment: prod (matometa), staging (autometa-staging), dev (default). Seeds review/one-click apps; prod and staging set it via scalingo env-set",
      "value": "staging"
    }
  }
}
